# Internal / dev-only artifacts — must NOT be deployed to the public web root.
# (These are local scratch dirs; safe to keep excluded even if regenerated.)
uploads/
screenshots/
.image-slots.state.json
