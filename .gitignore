# OS / editor
.DS_Store

# Vercel
.vercel/

# Dependencies (none currently, but just in case)
node_modules/

# Internal / dev artifacts — not part of the deployed site
.image-slots.state.json
screenshots/
uploads/

# Claude/dev tooling
.claude/
serve.py

# Local secrets — never commit
.env.local
__pycache__/
